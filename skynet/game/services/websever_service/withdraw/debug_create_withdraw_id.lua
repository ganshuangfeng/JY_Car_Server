local skynet = require "skynet_plus"
local cjson = require "cjson"
local basefunc = require "basefunc"
require "printfunc"

echo(cjson.encode({result=1002,error="not support, please use 'create_withdraw_id'  now!"}))
